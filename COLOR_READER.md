# 彩色阅读定制版

本分支以 Legado 的 KMP/Compose 阅读器为基础，加入阅读背景、正文配色、关键词/成对符号/正则着色和手动高亮。

## 默认界面

新安装或没有有效阅读配置时，第一个内置样式使用浅米色阅读预设：

| 设置 | 默认值 |
|---|---|
| 正文字号 | 21 |
| 字间距 | 0.06 em |
| 行距/段距 | 界面显示 0.3 / 0.5（配置存储值 13 / 5，行高乘数为 1.3） |
| 正文四边边距 | 25 |
| 首行缩进 | 两个全角空格 |
| 白天正文/背景 | `#3E3D3B` / `#DCD9C6` |
| 夜间正文/背景 | `#ADADAD` / `#000000` |
| 翻页动画 | 滑动（横向） |
| 标题 | 左对齐，字号增量 2，上间距 24 |

页眉显示时间和电量，页脚显示章节名和页码；默认不显示页眉页脚分隔线。墨水屏保留白底黑字和无动画。

已有配置中明确保存的值会保留，缺少的字段采用默认值。之后在界面设置中修改的值仍会保存，不会在每次启动时被重置。

要在已有安装中恢复这套界面：打开阅读菜单 → **界面** → 长按第一个样式 → **恢复** → 选择第一个内置样式（界面名称为“微信读书”）。

## 背景与高亮

- 阅读菜单 → 更多 → **阅读器配色**：编辑日间、夜间和墨水屏配色，导入背景图片，调整图片大小、位置、平铺和混合方式。点击应用保存，取消放弃草稿。
- 阅读菜单 → 更多 → **颜色规则**：管理关键词、成对符号和正则规则，设置文字色、背景色、粗体及下划线。
- 阅读页长按文字：选中后设置高亮、文字色或背景色。取消手势选区不会删除已保存的标注。

图片导入支持 PNG、JPG/JPEG、WebP，单张上限 12 MiB，每次编辑最多导入 32 张。修改底色不会自动删除背景图片；移除图片可保留底色。

## 安装与更新

Android 定制包关闭官方版本检查、启动更新弹窗、更新渠道和自动检查入口，避免官方安装包覆盖定制功能。书籍刷新、书源操作和规则订阅不受此限制。

需要升级时，从[本仓库](https://github.com/senyayume/legado)的构建产物或发布页获取新的定制安装包。保留数据的覆盖安装要求安装身份与签名一致；共存安装会使用独立数据。

日常使用应选择开启发布优化的安装包，调试包仅用于功能调试。构建方式见 [Android 构建说明](BUILDING_ANDROID.md)。

## 当前范围

这不是 ColorTxt 的全部桌面功能移植。原生动态色槽/词组导入、整包配色交换、全部纹理和命名方案尚未完成。新增颜色规则和高亮表的完整备份恢复合并仍待支持，不能把现有备份功能当作这些数据的完整迁移保证。

基础项目：[Legado](https://github.com/huajideshutiao/legado)；功能参考：[ColorTxt](https://github.com/ssnangua/ColorTxt)。
